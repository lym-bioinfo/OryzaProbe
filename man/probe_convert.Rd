% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probe-convert.R
\name{probe_convert}
\alias{probe_convert}
\title{Convert rice microarray probe IDs to annotated targets}
\usage{
probe_convert(
  x,
  platform = "auto",
  merge_by = "none",
  unmapped = "keep",
  non_rap = "keep",
  multi_mapping = "keep",
  na_rm = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{x}{A numeric matrix or data frame. Row names must contain probe IDs.}

\item{platform}{One of \code{"auto"}, \code{"GPL6864"},
\code{"GPL2025"}, or \code{"GPL8852"}. Automatic detection uses all row
names. Supplying the platform explicitly is recommended for production
analyses.}

\item{merge_by}{One of \code{"none"}, \code{"mean"}, \code{"max"}, or
\code{"min"}.}

\item{unmapped}{How probes without a target should be handled: keep the
original probe ID, drop the row, or raise an error.}

\item{non_rap}{How annotations that are not canonical RAP-DB gene IDs should
be handled: keep, drop, or raise an error.}

\item{multi_mapping}{How targets containing multiple IDs separated by
\code{///} should be handled: keep the combined label, use the first target,
expand the row once per target, drop it, or raise an error.}

\item{na_rm}{Whether missing values should be removed during aggregation. If
every value in a group is missing, the result remains \code{NA}.}

\item{quiet}{If \code{TRUE}, suppress the conversion summary message.}
}
\value{
A numeric matrix with class \code{oryza_probe_matrix} and unique row names. Use
\code{probe_conversion_report()} to inspect how each input row was handled.
}
\description{
Converts probe IDs stored in expression-matrix row names to targets from the
annotation bundled with OryzaProbe. Conversion preserves input order, records
an auditable row-level report, and can merge probes that resolve to one target.
}
\examples{
expression <- matrix(
  c(2, 4, 6, 8),
  nrow = 2,
  dimnames = list(c("36144", "37310"), c("sample_1", "sample_2"))
)

converted <- probe_convert(
  expression,
  platform = "GPL6864",
  merge_by = "mean"
)
converted
probe_conversion_report(converted)
}
